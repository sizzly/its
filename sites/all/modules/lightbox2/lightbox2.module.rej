--- lightbox2.module
+++ lightbox2.module
@@ -958,12 +958,7 @@
     $lightbox_style = NULL;
   }
 
-  if ($entity_type == 'node') {
-    $node_id = $entity->nid;
-  } 
-  else {
-    $node_id = 0;
-  }
+  $wrapper = entity_metadata_wrapper($entity_type, $entity);
 
   foreach ($items as $delta => $item) {
     $uri = array(
